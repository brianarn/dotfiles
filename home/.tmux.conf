# Set prefix to ctrl-a, freeing up ctrl-b
set-option -g prefix C-a
unbind-key C-b
bind-key C-a send-prefix

### Settings

# Terminal identity (prefer tmux-256color, fallback to screen-256color)
if-shell 'infocmp -x tmux-256color >/dev/null 2>&1' \
  'set-option -g default-terminal "tmux-256color"' \
  'set-option -g default-terminal "screen-256color"'

# Truecolor support for common terminal types
set-option -as terminal-features ',xterm-256color:RGB,screen-256color:RGB,tmux-256color:RGB'

# Keep a lot of scrollback buffer
set-option -g history-limit 50000

# Increase display of messages to 4 seconds
set-option -g display-time 4000

# Refresh status more often
set-option -g status-interval 5

# Focus events where supported
set-option -g focus-events on

# Index windows and panes starting at 1
set-option -g base-index 1
set-window-option -g pane-base-index 1

# Automatically renumber windows
set-option -g renumber-windows on

# Vi mode for copy and status
set-window-option -g mode-keys vi
set-option -g status-keys vi

# Enable mouse support
set-option -g mouse on

# Highlight activity in status bar
set-window-option -g monitor-activity on

# Native clipboard integration (OSC 52)
set-option -g set-clipboard on

# Allow escape sequences to pass through to the terminal (images, etc.)
set-option -g allow-passthrough on

### Key Bindings

## General

# Reload config
bind-key r source-file ~/.tmux.conf \; display-message "Reloaded tmux.conf"

# Toggle last active window
bind-key C-a last-window

## Windows

# Previous window
bind-key -r C-h select-window -t :-
# Next window
bind-key -r C-l select-window -t :+

## Panes

# Split pane left/right
unbind-key %
bind-key | split-window -h
# Split pane top/bottom
unbind-key '"'
bind-key - split-window -v

# Navigate panes (vim-style)
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

# Resize panes (vim-style, repeatable)
bind-key -r H resize-pane -L 5
bind-key -r J resize-pane -D 5
bind-key -r K resize-pane -U 5
bind-key -r L resize-pane -R 5

## Copy mode (vim-style)

# Begin selection
bind-key -T copy-mode-vi v send -X begin-selection
# Toggle rectangle selection
bind-key -T copy-mode-vi V send -X rectangle-toggle
# Yank selection and exit copy mode
bind-key -T copy-mode-vi y send -X copy-selection-and-cancel

### Status Bar

set-option -g status-left-length 40
set-option -g status-left "#[fg=brightgreen][#S] #[fg=blue]#I:#[fg=cyan]#P"
set-option -g status-right "#[fg=cyan]%d %b %R"
set-option -g status-justify left

### Colors (Flat UI inspired)
# See http://designmodo.github.io/Flat-UI/

# Status bar
set-option -g status-style "bg=#2C3E50,fg=#ECF0F1"

# Inactive window titles
set-window-option -g window-status-style "fg=#D35400,bg=default,dim"

# Active window title
set-window-option -g window-status-current-style "fg=#F1C40F,bg=default,bold"

# Pane borders
set-option -g pane-border-style "fg=#34495E,bg=#2C3E50"
set-option -g pane-active-border-style "fg=#1ABC9C,bg=#C0392B"

# Messages
set-option -g message-style "fg=#F39C12,bg=#2C3E50"

# Pane number display
set-option -g display-panes-active-colour "#2980B9"
set-option -g display-panes-colour "#2980B9"

# Clock
set-window-option -g clock-mode-colour "#C0392B"
